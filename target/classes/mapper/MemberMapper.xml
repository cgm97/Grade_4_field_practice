<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
   <!-- 이름 겸색 테스트 -->
   <select id="getListMember" resultType="memberDTO">
	  	 SELECT * 
	  	 	FROM MEMBER 
   </select>

</mapper>